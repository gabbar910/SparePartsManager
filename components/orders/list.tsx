import Image from "next/image";
import Link from "next/link";
import { useState } from "react";

const ListOrders = () => {

    const [isOpen, setIsOpen] = useState(false);

    return (
            <div>
                <span>Order listing page</span>
            </div>
        );

};

export default ListOrders;